# 知乎专栏粘贴说明

知乎不渲染 mermaid，也不显示 SVG。本文配图已是 PNG，按顺序在编辑器里上传后，再贴正文。

## 标题

三个 AI 互相挑剔并不难。难的是让它们停下来。

## 导语（30–40 字，可作开头摘要）

多智能体真正难的不是让它们说话，是让它们停。停有定义，停有账单。数字一旦便宜得不像话，先查它们是不是没干活。

## 配图上传顺序

1. `images/01-architecture.png` — 控制面集中，数据面点对点
2. `images/02-stop-condition.png` — 停机条件：批判撤销满意
3. `images/03-cost-law.png` — 串行重读 vs 并行买不到 token
4. `images/04-triangle-rectangle.png` — 0.18× 是三角形 bug，修后变矩形

正文里的 `![…](images/….png)` 粘贴后不会自动带上本地文件；在对应位置插入刚上传的图即可。公式已改成纯文本（pΣ、ê、N²），不要依赖 Markdown 数学语法。

## 标签建议

Agent、多智能体、系统设计、开源
